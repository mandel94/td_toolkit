"""Additional utility dependencies for CLI."""

from typing import Any

# Add typer and rich to pyproject.toml dependencies
ADDITIONAL_DEPS = [
    "typer[all]>=0.9.0",
    "rich>=13.7.0",
    "numpy>=1.26.0",
]
